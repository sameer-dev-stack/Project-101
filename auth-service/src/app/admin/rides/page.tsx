import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function RidesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Rides</CardTitle>
        <CardDescription>View and manage all ongoing and completed rides.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Rides management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
